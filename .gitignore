node_modules
.next
out
.DS_Store
*.log
.env*.local
.vercel
next-env.d.ts.bak

# Local IDE / harness config (machine-specific paths)
.claude/

# User-provided working documents
feedback*.docx

# Raw V&V site (250MB local copy used to take screenshots — not the portfolio itself)
public/vv-site/
# Raw NexGen practice site (same idea — local source for screenshots only)
public/nex-site/
